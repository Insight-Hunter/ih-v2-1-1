import React from 'react';
import Onboarding from './onboarding/Onboarding';

const App: React.FC = () => <Onboarding />;

const App: React.FC = () => {
  return (
    <div className="app-container">
      <h1>Welcome to Insight Hunter</h1>
      <p>Starting development on Cloudflare Workers with React + Vite + TypeScript.</p>
    </div>
  );
};

export default 
